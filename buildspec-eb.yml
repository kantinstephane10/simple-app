version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - mvn -B clean package -DskipTests
      - cp target/simple-app-*.jar app.jar

artifacts:
  files:
    - app.jar
    - Procfile
  discard-paths: yes
