version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - mvn -B clean package -DskipTests

artifacts:
  files:
    - target/simple-app-*.jar
  discard-paths: yes
